import React from 'react';

import './styles.scss';

const CustomTextArea = ({name}) => {
    return(


        <div className="responsive-container custom-area">
            <label>Informações</label>
           <textarea>
        
            </textarea>
        </div>   
    )
};

export default CustomTextArea;